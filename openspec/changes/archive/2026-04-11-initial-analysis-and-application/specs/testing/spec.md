## ADDED Requirements

### Requirement: Provide automated tests for core features
The system SHALL include smoke and integration tests for major features, runnable via the Makefile.

#### Scenario: Tests run successfully
- **WHEN** the user runs the test command
- **THEN** all core tests pass and results are reported
