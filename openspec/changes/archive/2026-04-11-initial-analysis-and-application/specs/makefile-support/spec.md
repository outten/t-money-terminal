## ADDED Requirements

### Requirement: Provide a Makefile for setup and maintenance
The system SHALL include a Makefile with directives for running, testing, and maintaining the application.

#### Scenario: Makefile present
- **WHEN** the user lists project files
- **THEN** a Makefile is present with commands for build, run, and test
